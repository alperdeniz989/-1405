name: Run Book

on:
  push:
    branches:
      - main
  workflow_dispatch:

jobs:
  test:
    runs-on: ubuntu-latest

    steps:
      - name: Get code
        uses: actions/checkout@v4

      - name: Check files
        run: |
          echo "===================="
          echo "BOOK PROJECT STARTED"
          echo "===================="
          echo "Files in repository:"
          ls -la

      - name: Finish
        run: echo "SUCCESS - Project is running"
